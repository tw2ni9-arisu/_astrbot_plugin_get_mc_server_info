# Changelog

按时间倒序记录版本更新。文件名与 git tag 对应。

---

## v2.0.3

- 服务器 icon：兼容 mcstatus 14.x 将 `JavaStatusResponse.favicon` 改名为 `JavaStatusResponse.icon`，插件同时读取新旧字段（`status.favicon or status.icon`），修复部分 mcstatus 版本下服务器 favicon 永远拿不到导致首页图标显示 MC 占位的问题。
- 玩家头像：默认 `skin_api_url_template` 由 `byuuid/{uuid}` 改为 `byname/{name}`（mua union 接口正规范式），优先按玩家名获取皮肤；UUID 作为 fallback 继续走 `byuuid/{uuid}`，保证 union 端无 name 记录时仍可命中。修复 union API 对按 UUID 查询一律 404 导致玩家头像全部走灰色占位的问题。
- `_normalize_skin_api_url_template` 兼容 `{name}` / `{uuid}` 两种占位，非法模板仍回退到默认值。

## v2.0.2

- 群聊权限改为按功能独立配置，查询、添加、备用线路、删除、重命名、列表、重定向和模板操作等功能可分别设置是否要求群管理员权限。
- 命令与对应 LLM Tool 共用同一权限配置；私聊不要求管理员角色，`/数据清除` 仍始终仅群管理员可用。
- 已保存服务器查询、未保存地址直连和历史状态查询使用三个独立开关，可按风险分别控制。
- 新增 `default_offline_motd`，可修改服务器离线且没有历史 MOTD 时显示的默认文案。
- 配置项说明和提示全部改为中文；旧版 `mutation_requires_admin` 不再显示，首次加载 v2.0.2 时会一次性迁移到对应的独立权限项。

## v2.0.1

- 新增 `/备用线路`、`/备用` 和 `/bak`，可为会话内已有服务器保存按添加顺序排列的备用线路；保存前会先验证地址能否访问。
- 主动查询已保存服务器时，优先访问主线路；主线路不可达时依次尝试备用线路，全部失败则返回 Offline 状态图。
- 无参数 `/查询` 会主动查询当前会话全部服务器，并使用实际响应线路更新服务器图标、玩家和延迟后生成合并图片。
- `/列表` 改为完全读取已有缓存，不会主动访问主线路或备用线路；列表图同时展示主线路、全部备用线路和最近缓存状态。
- 静默轮询会查询服务器的全部主备线路，并将成功结果中的最大延迟写入历史；所有线路均不可达时记录为 Offline。
- 模板改为目录化主/副模板结构：单服查询使用主模板，列表与全量查询使用副模板；内置终端副模板直接复用默认副模板。
- 新增 LLM Tool `add_mc_server_backup`。
- 修复 AstrBot 以 `data.plugins` 命名空间加载插件时，`/查询` 和 `/列表` 无法导入副模板的问题。
